#include "bai3.h"
#include "bai4.h"
using namespace std;
int main() {
	Vector <int> a;
	a.push_back(10);
	a.push_back(11);
	for (int i = 0; i < a.size(); i++) cout << a[i] << ' ';
	
	cout << "Hello world";
}